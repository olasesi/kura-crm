apiVersion: v1
kind: Service
metadata:
  name: kura-crm-api
  namespace: kura-crm
  labels:
    app: kura-crm
    tier: api
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: kura-crm
    tier: api
